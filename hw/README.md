# hw

Hardware design workspace for PCB, enclosure, BOM, and manufacturing notes.

The existing `HW/` directory is kept for compatibility with earlier project notes. New task-list paths should prefer this lowercase `hw/` directory unless the team decides otherwise.
